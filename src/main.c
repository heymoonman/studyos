#include <stdio.h>


int main(int argc, char **argv) {
    printf("woww±!!!!\n");
    printf("we built this with make!!!\n");

    for (int i = 0; i < 20; i++) {
        printf("we are at %d\n", i);
    }

    return 0;
}
